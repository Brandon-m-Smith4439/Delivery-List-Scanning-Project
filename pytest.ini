# File: pytest.ini
# Project test markers. The visual suite renders the real HTML/CSS/JavaScript with
# mocked API data because this execution environment blocks Chromium localhost navigation.
[pytest]
markers =
    visual: browser-rendered visual and interaction smoke tests
