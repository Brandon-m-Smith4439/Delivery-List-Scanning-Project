# File: pytest.ini
# Project test markers and discovery scope. Verification copies may contain complete
# project trees and vendored packages, so they must never be collected as project tests.
[pytest]
testpaths = tests
norecursedirs = .git .venv venv _verification backups node_modules
markers =
    visual: browser-rendered visual and interaction smoke tests
